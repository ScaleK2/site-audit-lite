import {createRun}from'../output/run.js';import {captureUrls}from'../capture/browser.js';import {allowedLink}from'../capture/links.js';import {parseUrlList}from'../input/url-list.js';import {loadConfig}from'../input/config.js';import {ingestHar}from'../har/ingest.js';import {diagnose}from'../diagnostics/index.js';import {writeReports}from'../output/write.js';
export async function captureList({urls,outputRoot,waitMs}){const parsed=await parseUrlList(urls),valid=parsed.filter(x=>x.valid).map(x=>x.value),errs=parsed.filter(x=>!x.valid).map(x=>err('capture-list',x.value,'invalid_url','Invalid HTTP(S) URL'));if(!valid.length)throw new Error('URL list contains no valid HTTP(S) URLs');return finish(await createRun({domain:valid[0],mode:'capture-list',inputs:{urls},root:outputRoot}),valid,{waitMs},errs)}
export async function captureSite({url,maxUrls=100,includeSubdomains=false,outputRoot,waitMs}){const run=await createRun({domain:url,mode:'capture-site',inputs:{url,max_urls:maxUrls,include_subdomains:includeSubdomains},root:outputRoot});const seen=new Set(),queue=[url],all={harFiles:[],forms:[],templates:[],errors:[],completed:0,interactions:0,submissions:0};while(queue.length&&seen.size<maxUrls){const u=queue.shift();if(seen.has(u))continue;seen.add(u);const c=await import('../capture/browser.js').then(m=>m.captureUrls(run,[u],{waitMs,discover:true}));merge(all,c);for(const raw of c.discovered||[]){const x=allowedLink(raw,url,{includeSubdomains});if(x&&!seen.has(x)&&!queue.includes(x)&&seen.size+queue.length<maxUrls)queue.push(x)}}return finalise(run,all,[],{discovered_count:seen.size})}
export async function captureConfig({config,urls,confirmSubmissions=false,outputRoot,waitMs}){const cfg=await loadConfig(config),parsed=urls?await parseUrlList(urls):cfg.interactions.map((i,index)=>({value:i.url,index,valid:true})),valid=parsed.filter(x=>x.valid).map(x=>x.value),errs=parsed.filter(x=>!x.valid).map(x=>err('capture-config',x.value,'invalid_url','Invalid HTTP(S) URL'));const run=await createRun({domain:cfg.domain||valid[0],mode:'capture-config',inputs:{config,urls,confirm_submissions:confirmSubmissions},root:outputRoot});return finish(run,valid,{waitMs,interactions:cfg.interactions,confirmSubmissions},errs)}
async function finish(run,urls,opts,errs){const {captureUrls}=await import('../capture/browser.js');return finalise(run,await captureUrls(run,urls,opts),errs)}
async function finalise(run,c,initial=[],extra={}){let requests=[];for(const f of c.harFiles)try{requests.push(...await ingestHar(f,{runId:run.runId,primaryDomain:run.primaryDomain,capturedAt:run.startedAt}))}catch(e){c.errors.push(err(run.mode,null,'har_ingestion',e.message,f))}diagnose(requests);const errors=[...initial,...c.errors];const summary=await writeReports(run,{requests,forms:c.forms,templates:c.templates,errors,summary:{...extra,attempted_count:c.harFiles.length,completed_count:c.completed,failed_count:c.harFiles.length-c.completed,interaction_count:c.interactions,submission_count:c.submissions,har_files:c.harFiles,domains:[...new Set(requests.map(r=>r.hostname))]}});return{run,summary}}
function merge(a,b){for(const k of['harFiles','forms','templates','errors'])a[k].push(...b[k]);for(const k of['completed','interactions','submissions'])a[k]+=b[k];a.discovered=b.discovered}
const err=(mode,page_url,category,message,source_har=null)=>({timestamp:new Date().toISOString(),mode,page_url,source_har,interaction:null,action_index:null,category,message:String(message).slice(0,500),continued:true});
